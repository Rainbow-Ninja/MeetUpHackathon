<div id="welcomePage">
    <h1>Welcome</h1>
    <a href="/event/new">Create New Event</a>

    {{#each event}}
        <div class="card" style="width: 18rem;">
            <img class="card-img-top" src=" " alt="Card image cap">
            <div class="card-body">
                <h5 class="card-title">{{this.title}}</h5>
                <p class="card-text">{{this.details}}</p>
            </div>
            <ul class="list-group list-group-flush">
                <li class="list-group-item">Address: {{this.address}}</li>
                <li class="list-group-item">Time: {{this.startTime}} - {{this.endTime}}</li>
                <li class="list-group-item">Date: {{this.date}}</li>
            </ul>
            <div class="card-body">
                <a href="/event/{{this._id}}" class="card-link">Read More</a>
            </div>
        </div>



        {{!-- <div id="eventCard">
            <div class="shadow card">
                <h5 class="card-header">{{ this.title }}</h5>
                <div class="card-body">
                    <p class="card-text">{{ this.content }}</p>
                    <a class="btn btn-sm btn-primary" href="/blog/{{this._id}}">Keep Reading</a>
                    <a class="btn btn-sm btn-primary" href="/blog/{{this._id}}?_method=DELETE">Delete Blog</a>
                    <a class="btn btn-sm btn-primary" href="/blog/edit/{{this._id}}">Edit Blog</a>
                </div>
            </div>
        </div> --}}
    {{/each}}
</div>